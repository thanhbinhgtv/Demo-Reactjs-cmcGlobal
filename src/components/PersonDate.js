import React from "react";
import "./PersonDate.css";

const PersonDate = (props) => {
  return (
    <div>
      <div className="abc">PersonDate : {props.hello}</div>
    </div>
  );
};

export default PersonDate;
